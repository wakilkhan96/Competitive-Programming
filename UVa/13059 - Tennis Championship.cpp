#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long int x,match;
    while(scanf("%lld",&x)==1 && x!=0)
    {
        match=x-1;
        printf("%lld\n",match);
    }
    return 0;
}
